// Tailwind v4 usa @import "tailwindcss" — no necesita content config.
// Este archivo se mantiene por compatibilidad pero no se usa para escaneo.
import type { Config } from 'tailwindcss'

const config: Config = {}

export default config